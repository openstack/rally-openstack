description: Test scenario for creating volumes and updating them in Cinder block
  storage service.
subtasks:
- contexts:
    users:
      tenants: 1
      users_per_tenant: 1
  runner:
    constant:
      concurrency: 1
      times: 3
  scenario:
    CinderVolumes.create_and_update_volume:
      create_volume_kwargs: {}
      size: 1
      update_volume_kwargs:
        description: desc_updated
  sla:
    failure_rate:
      max: 0
  title: Create And Update Volume
title: Cinder Volume Creation and Update
version: 2
