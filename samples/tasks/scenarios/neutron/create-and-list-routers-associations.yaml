{% set flavor_name = flavor_name or "m1.tiny" %}
---
version: 2
title: Neutron Create And List Routers Associations
description: Test creating Neutron networking resources
subtasks:
- title: Create and list routers associations
  scenario:
    NeutronBGPVPN.create_and_list_routers_associations: {}
  runner:
    constant:
      times: 10
      concurrency: 2
  contexts:
    users:
      tenants: 1
      users_per_tenant: 1
    network: {}
    servers:
      servers_per_tenant: 1
      auto_assign_nic: true
      flavor:
        name: '{{flavor_name}}'
      image:
        name: ^cirros.*-disk$
  sla:
    failure_rate:
      max: 0
